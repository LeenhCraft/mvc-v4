{% extends "layouts/base.twig" %}

{% block content %}
<div class="min-h-screen flex items-center justify-center bg-gray-100 px-4">
    <div class="max-w-lg w-full text-center">
        <div class="mb-8">
            <div class="inline-block p-6 bg-red-100 rounded-full mb-4">
                <svg class="w-24 h-24 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                </svg>
            </div>
            <h1 class="text-6xl font-bold text-red-600">500</h1>
        </div>

        <h2 class="text-3xl font-bold text-gray-800 mb-4">
            Error Interno del Servidor
        </h2>

        <p class="text-gray-600 mb-8">
            Algo salió mal en nuestro servidor. Estamos trabajando para solucionarlo.
        </p>

        <div class="bg-white rounded-lg shadow-md p-6 mb-8">
            {% if error_message and show_details %}
                <div class="mb-4">
                    <p class="text-sm font-semibold text-gray-700 mb-2">Detalles del Error:</p>
                    <div class="bg-red-50 border border-red-200 rounded p-3 text-left">
                        <code class="text-sm text-red-800 break-words">{{ error_message }}</code>
                    </div>
                </div>
            {% endif %}

            {% if error_file and show_details %}
                <div class="mb-4">
                    <p class="text-sm font-semibold text-gray-700 mb-2">Archivo:</p>
                    <code class="block bg-gray-100 px-3 py-2 rounded text-xs text-gray-700 break-all">
                        {{ error_file }}{% if error_line %}:{{ error_line }}{% endif %}
                    </code>
                </div>
            {% endif %}

            <div>
                <p class="text-sm text-gray-500">
                    <strong>Timestamp:</strong> {{ "now"|date("Y-m-d H:i:s") }}
                </p>
                {% if request_id %}
                <p class="text-xs text-gray-400 mt-2">
                    ID: <code>{{ request_id }}</code>
                </p>
                {% endif %}
            </div>
        </div>

        <div class="space-y-3">
            <a href="/" class="block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200">
                <svg class="inline-block w-5 h-5 mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                </svg>
                Volver al inicio
            </a>

            <button onclick="location.reload()" class="block w-full bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold py-3 px-6 rounded-lg transition duration-200">
                <svg class="inline-block w-5 h-5 mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                </svg>
                Reintentar
            </button>
        </div>

        <div class="mt-8 p-4 bg-blue-50 border border-blue-200 rounded-lg">
            <p class="text-sm text-blue-800 mb-2">
                <strong>💡 Qué puedes hacer:</strong>
            </p>
            <ul class="text-sm text-blue-700 text-left space-y-1">
                <li>• Intenta recargar la página en unos momentos</li>
                <li>• Verifica tu conexión a internet</li>
                <li>• Si el problema persiste, contacta al soporte</li>
            </ul>
        </div>

        {% if not show_details %}
        <p class="mt-6 text-xs text-gray-400">
            Los detalles técnicos están ocultos en modo producción
        </p>
        {% endif %}
    </div>
</div>
{% endblock %}
